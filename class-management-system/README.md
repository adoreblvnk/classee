generate build directory

`cmake -S . -B build`

build (the executable)

`cmake --build build`

run

`./build/src/main`

or

`.\build\bin\cms.exe`

clean

`rm -rf build`

or

delete the build directory
